package testy.domain.question.image;

import testy.domain.question.AbstractAnswerValidator;

public class ImageAnswerValidator extends AbstractAnswerValidator<ImageAnswer> {

	@Override
    public int validate(ImageAnswer answer) {
	    return 0;
    }

}
